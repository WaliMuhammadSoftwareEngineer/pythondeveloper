comments:
  allowedFields:
    - name
    - email
    - comment
  branch: "main"
  commitMessage: "Add comment by {fields.name}"
  filename: "comments/{@timestamp}"
  format: "json"
  path: "data/comments"
  requiredFields:
    - name
    - email
    - comment
